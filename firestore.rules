rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Każdy zalogowany użytkownik czyta i zapisuje wyłącznie własny dokument.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
